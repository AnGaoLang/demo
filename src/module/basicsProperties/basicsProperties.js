export default class properties {
  // id = ''// 组件外层container的id
  // type = ''// 组件名
  selected = false// 是不是选中了
  style = {
    width: 100,
    height: 100,
    top: 0,
    left: 0,
    rotate: 0
    // color: '',
    // lineHeight: '',
  }
  selectAble = true// 是否能选择：false
  moveable = true// 是否能移动：false
  resizeable = true// 是否能进行放大缩小：false
  // rotateable = true// 是否能旋转：false
  // copyable = true// 是否有能进行复制：false
}
